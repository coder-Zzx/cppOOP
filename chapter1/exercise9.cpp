/*变量名要用有意义的名字!!!
 *变量名要用有意义的名字!!!
 *变量名要用有意义的名字!!!*/
#include<iostream>
#include<string>
using namespace std;

int main()
{
    string result="",input="";
    while (cin>>input)
    {
        result+=input;
    }
    cout<<result;
    // system("pause");
    return 0;
}